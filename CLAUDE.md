# omnis - Project Instructions

## Python Tooling
- Use `uv` as the Python package manager (not `pip`)
- Commands: `uv add <pkg>`, `uv sync`, `uv run <script>`
- Always use up-to-date, non-deprecated packages and APIs
